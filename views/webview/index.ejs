<!-- Centered page tabs -->
<div class="container">
  <div class="page-tabs">
    <button class="page-tab active" id="tabHome" onclick="switchTab('home')">Home</button>
    <button class="page-tab" id="tabCategory" onclick="switchTab('category')">Category</button>
    <span class="tab-indicator" id="tabIndicator"></span>
  </div>
</div>

<!-- ══ HOME TAB ════════════════════════════════════════════ -->
<div id="homeContent" class="tab-content">

  <!-- Full-width hero carousel (outside container so it can bleed to screen edges on desktop) -->
  <div class="promo-carousel" id="promoCarousel">

    <div class="promo-slide promo-slide--1 active">
      <img class="promo-slide__bg-img"
           src="https://images.unsplash.com/photo-1512941937669-90a1b58e7e9c?w=1920&q=90"
           alt="">
      <div class="promo-slide__overlay"></div>
      <div class="promo-slide__content">
        <span class="promo-slide__tag">Fast &amp; Easy</span>
        <h2 class="promo-slide__title">Buy Data Bundles in Seconds</h2>
        <p class="promo-slide__sub">Instant delivery to any Nigerian number</p>
        <a href="/category/data-category" class="promo-slide__btn">Shop Now</a>
      </div>
    </div>

    <div class="promo-slide promo-slide--2">
      <img class="promo-slide__bg-img"
           src="https://images.unsplash.com/photo-1451187580459-43490279c0fa?w=1920&q=90"
           alt="">
      <div class="promo-slide__overlay"></div>
      <div class="promo-slide__content">
        <span class="promo-slide__tag">Always Available</span>
        <h2 class="promo-slide__title">24/7 Data, Never Run Out</h2>
        <p class="promo-slide__sub">MTN · Airtel · Glo · 9Mobile</p>
        <a href="/category/data-category" class="promo-slide__btn">Browse Plans</a>
      </div>
    </div>

    <div class="promo-slide promo-slide--3">
      <img class="promo-slide__bg-img"
           src="https://images.unsplash.com/photo-1607082349566-187342175e2f?w=1920&q=90"
           alt="">
      <div class="promo-slide__overlay"></div>
      <div class="promo-slide__content">
        <span class="promo-slide__tag">Earn Rewards</span>
        <h2 class="promo-slide__title">Get Reward Points on Every Purchase</h2>
        <p class="promo-slide__sub">Redeem RP for free data bundles</p>
        <a href="/category/data-category" class="promo-slide__btn">Start Earning</a>
      </div>
    </div>

  </div>

  <!-- Slide counter badge (desktop only) — sits at bottom-left corner of inset image -->
  <div class="promo-slide__badge" id="slideBadge">
    <span class="promo-slide__badge-num" id="slideBadgeNum">01</span>
    <span class="promo-slide__badge-sep">/ 03</span>
  </div>

  <!-- Dot indicators — in container so they're centred / aligned -->
  <div class="container">
    <div class="promo-dots" id="promoDots">
      <span class="promo-dot promo-dot--active"></span>
      <span class="promo-dot"></span>
      <span class="promo-dot"></span>
    </div>
  </div>

  <!-- ── Desktop: Browse Categories (hidden on mobile) ───── -->
  <div class="desktop-section cat-browse">
    <div class="container">
      <div class="cat-browse__heading">
        <h2 class="cat-browse__title">Browse Categories</h2>
        <p class="cat-browse__subtitle">Everything you need, all in one place</p>
      </div>
      <div class="cat-browse__grid">

        <a href="/category/data-category" class="cat-browse__card">
          <div class="cat-browse__img-wrap">
            <img src="https://images.unsplash.com/photo-1512941937669-90a1b58e7e9c?w=800&q=85" alt="Data Bundles">
            <div class="cat-browse__hover-overlay">
              <div class="cat-browse__hover-label">Data Bundles</div>
              <div class="cat-browse__hover-desc">MTN · Airtel · Glo · 9Mobile</div>
            </div>
          </div>
        </a>

        <a href="/category/electronic-category" class="cat-browse__card">
          <div class="cat-browse__img-wrap">
            <img src="https://images.unsplash.com/photo-1498049794561-7780e7231661?w=800&q=85" alt="Electronics">
            <div class="cat-browse__hover-overlay">
              <div class="cat-browse__hover-label">Electronics</div>
              <div class="cat-browse__hover-desc">Phones, Laptops &amp; Gadgets</div>
            </div>
          </div>
        </a>

        <a href="/category/automobile-category" class="cat-browse__card">
          <div class="cat-browse__img-wrap">
            <img src="https://images.unsplash.com/photo-1503376780353-7e6692767b70?w=800&q=85" alt="Automobiles">
            <div class="cat-browse__hover-overlay">
              <div class="cat-browse__hover-label">Automobiles</div>
              <div class="cat-browse__hover-desc">Cars, Parts &amp; Accessories</div>
            </div>
          </div>
        </a>

        <a href="/category/course-category" class="cat-browse__card">
          <div class="cat-browse__img-wrap">
            <img src="https://images.unsplash.com/photo-1434030216411-0b793f4b4173?w=800&q=85" alt="Courses">
            <div class="cat-browse__hover-overlay">
              <div class="cat-browse__hover-label">Courses</div>
              <div class="cat-browse__hover-desc">Online Skills &amp; Training</div>
            </div>
          </div>
        </a>

      </div>
    </div>
  </div>

  <!-- ── Products, strips, trust bar ─────────────────────── -->
  <div class="container">

    <!-- New Arrivals -->
    <div class="sec-header" style="margin-top:28px;">
      <span class="sec-title">New Arrivals <span style="font-size:16px;">🔥</span></span>
      <a href="/category/data-category" class="sec-link">See All</a>
    </div>

    <% if (dataProducts && dataProducts.length > 0) { %>
    <div class="product-grid" id="homeProductGrid">
      <% dataProducts.slice(0, 8).forEach(function(product) { %>
      <div class="product-card"
           data-name="<%= (product.dataDetails.plan_type + ' ' + product.dataDetails.plan_name).toLowerCase() %>"
           data-brand="<%= product.dataDetails.network.toLowerCase() %>">
        <div class="product-card__img">
          <img src="<%= product.images[0] %>" alt="<%= product.dataDetails.plan_type %>">
          <button class="product-card__fav" aria-label="Favourite">
            <i data-lucide="heart"></i>
          </button>
        </div>
        <div class="product-card__body">
          <h4 class="product-card__name"><%= product.dataDetails.plan_type %> · <%= product.dataDetails.plan_name %></h4>
          <p class="product-card__brand"><%= product.dataDetails.network %></p>
          <div class="product-card__footer">
            <span class="product-card__price">₦<%= product.dataDetails.amount %></span>
            <% if (user) { %>
            <button class="product-card__buy buy-btn" data-id="<%= product._id %>" aria-label="Buy">
              <i data-lucide="plus"></i>
            </button>
            <% } else { %>
            <a href="/user/login" class="product-card__buy" aria-label="Login to buy">
              <i data-lucide="plus"></i>
            </a>
            <% } %>
          </div>
        </div>
      </div>
      <% }) %>
    </div>
    <% } else { %>
    <div class="empty-state">
      <i data-lucide="package"></i>
      <p>No products available yet</p>
    </div>
    <% } %>

    <!-- ── Electronics strip ──────────────────────────────── -->
    <% if (electronicProducts && electronicProducts.length > 0) { %>
    <div class="cat-strip">
      <div class="cat-strip__head">
        <h3 class="cat-strip__title">
          <i data-lucide="monitor-smartphone"></i>
          Electronics
        </h3>
        <a href="/category/electronic-category" class="cat-strip__link">
          See All <i data-lucide="chevron-right"></i>
        </a>
      </div>
      <div class="cat-strip__scroll">
        <% electronicProducts.slice(0, 4).forEach(function(p) { %>
        <a href="/category/electronic-category" class="cat-strip__card">
          <% if (p.images && p.images[0]) { %>
          <img src="<%= p.images[0] %>" alt="<%= p.electronicDetails && p.electronicDetails.itemName || 'Electronic' %>" class="cat-strip__img">
          <% } else { %>
          <div class="cat-strip__img--placeholder"><i data-lucide="monitor"></i></div>
          <% } %>
          <div class="cat-strip__body">
            <div class="cat-strip__name"><%= p.electronicDetails && p.electronicDetails.itemName || p.description %></div>
            <div class="cat-strip__sub"><%= p.electronicDetails && p.electronicDetails.brandItem || '' %></div>
            <% if (p.electronicDetails && p.electronicDetails.items_price) { %>
            <div class="cat-strip__price">₦<%= p.electronicDetails.items_price.toLocaleString() %></div>
            <% } %>
          </div>
        </a>
        <% }) %>
        <% if (electronicProducts.length > 4) { %>
        <a href="/category/electronic-category" class="cat-strip__more">
          <i data-lucide="arrow-right"></i>
          More
        </a>
        <% } %>
      </div>
    </div>
    <% } %>

    <!-- ── Automobiles strip ───────────────────────────────── -->
    <% if (automobileProducts && automobileProducts.length > 0) { %>
    <div class="cat-strip">
      <div class="cat-strip__head">
        <h3 class="cat-strip__title">
          <i data-lucide="car"></i>
          Automobiles
        </h3>
        <a href="/category/automobile-category" class="cat-strip__link">
          See All <i data-lucide="chevron-right"></i>
        </a>
      </div>
      <div class="cat-strip__scroll">
        <% automobileProducts.slice(0, 4).forEach(function(p) { %>
        <a href="/category/automobile-category" class="cat-strip__card">
          <% if (p.images && p.images[0]) { %>
          <img src="<%= p.images[0] %>" alt="<%= p.automobileDetails && p.automobileDetails.brand || 'Auto' %>" class="cat-strip__img">
          <% } else { %>
          <div class="cat-strip__img--placeholder"><i data-lucide="car"></i></div>
          <% } %>
          <div class="cat-strip__body">
            <div class="cat-strip__name"><%= p.automobileDetails && (p.automobileDetails.brand + ' ' + p.automobileDetails.model) || p.description %></div>
            <div class="cat-strip__sub"><%= p.automobileDetails && (p.automobileDetails.year + ' · ' + (p.automobileDetails.condition || '')) || '' %></div>
            <% if (p.automobileDetails && p.automobileDetails.price) { %>
            <div class="cat-strip__price">₦<%= p.automobileDetails.price.toLocaleString() %></div>
            <% } %>
          </div>
        </a>
        <% }) %>
        <% if (automobileProducts.length > 4) { %>
        <a href="/category/automobile-category" class="cat-strip__more">
          <i data-lucide="arrow-right"></i>
          More
        </a>
        <% } %>
      </div>
    </div>
    <% } %>

    <!-- ── Courses strip ───────────────────────────────────── -->
    <% if (coursesProducts && coursesProducts.length > 0) { %>
    <div class="cat-strip">
      <div class="cat-strip__head">
        <h3 class="cat-strip__title">
          <i data-lucide="graduation-cap"></i>
          Courses
        </h3>
        <a href="/category/course-category" class="cat-strip__link">
          See All <i data-lucide="chevron-right"></i>
        </a>
      </div>
      <div class="cat-strip__scroll">
        <% coursesProducts.slice(0, 4).forEach(function(p) { %>
        <a href="/category/course-category" class="cat-strip__card">
          <% if (p.images && p.images[0]) { %>
          <img src="<%= p.images[0] %>" alt="<%= p.coursesDetails && p.coursesDetails.title || 'Course' %>" class="cat-strip__img">
          <% } else { %>
          <div class="cat-strip__img--placeholder"><i data-lucide="book-open"></i></div>
          <% } %>
          <div class="cat-strip__body">
            <div class="cat-strip__name"><%= p.coursesDetails && p.coursesDetails.title || p.description %></div>
            <div class="cat-strip__sub"><%= p.coursesDetails && p.coursesDetails.instructor ? 'by ' + p.coursesDetails.instructor : '' %></div>
            <% if (p.coursesDetails && p.coursesDetails.course_price) { %>
            <div class="cat-strip__price">₦<%= p.coursesDetails.course_price.toLocaleString() %></div>
            <% } %>
          </div>
        </a>
        <% }) %>
        <% if (coursesProducts.length > 4) { %>
        <a href="/category/course-category" class="cat-strip__more">
          <i data-lucide="arrow-right"></i>
          More
        </a>
        <% } %>
      </div>
    </div>
    <% } %>

    <!-- ── Trust bar (desktop only) ───────────────────────── -->
    <div class="desktop-section">
      <div class="trust-bar">

        <div class="trust-item">
          <div class="trust-item__icon">
            <i data-lucide="zap"></i>
          </div>
          <div>
            <div class="trust-item__title">Instant Delivery</div>
            <div class="trust-item__sub">Data bundles delivered in seconds to any Nigerian network</div>
          </div>
        </div>

        <div class="trust-item">
          <div class="trust-item__icon">
            <i data-lucide="shield-check"></i>
          </div>
          <div>
            <div class="trust-item__title">Secure Payments</div>
            <div class="trust-item__sub">All transactions are encrypted and fully protected</div>
          </div>
        </div>

        <div class="trust-item">
          <div class="trust-item__icon">
            <i data-lucide="headphones"></i>
          </div>
          <div>
            <div class="trust-item__title">24/7 Support</div>
            <div class="trust-item__sub">Our team is always available to help you out</div>
          </div>
        </div>

        <div class="trust-item">
          <div class="trust-item__icon">
            <i data-lucide="gift"></i>
          </div>
          <div>
            <div class="trust-item__title">Reward Points</div>
            <div class="trust-item__sub">Earn RP on every purchase and redeem for free data</div>
          </div>
        </div>

      </div>
    </div>

  </div>
</div>

<!-- ══ CATEGORY TAB ════════════════════════════════════════ -->
<div id="categoryContent" class="tab-content" style="display:none;">
  <div class="container">
    <div class="cat-banner-list">

      <a href="/category/data-category" class="cat-banner-card">
        <div class="cat-banner-card__info">
          <h3 class="cat-banner-card__name">Data Bundles</h3>
          <p class="cat-banner-card__count">MTN · Airtel · Glo · 9Mobile</p>
        </div>
        <div class="cat-banner-card__img">
          <img src="https://images.unsplash.com/photo-1607252650355-f7fd0460ccdb?w=250&q=80" alt="Data">
        </div>
      </a>

      <a href="/category/electronic-category" class="cat-banner-card">
        <div class="cat-banner-card__info">
          <h3 class="cat-banner-card__name">Electronics</h3>
          <p class="cat-banner-card__count">Phones, Laptops, Gadgets</p>
        </div>
        <div class="cat-banner-card__img">
          <img src="https://images.unsplash.com/photo-1498049794561-7780e7231661?w=250&q=80" alt="Electronics">
        </div>
      </a>

      <a href="/category/automobile-category" class="cat-banner-card">
        <div class="cat-banner-card__info">
          <h3 class="cat-banner-card__name">Automobiles</h3>
          <p class="cat-banner-card__count">Cars, Parts, Accessories</p>
        </div>
        <div class="cat-banner-card__img">
          <img src="https://images.unsplash.com/photo-1503376780353-7e6692767b70?w=250&q=80" alt="Automobiles">
        </div>
      </a>

      <a href="/category/course-category" class="cat-banner-card">
        <div class="cat-banner-card__info">
          <h3 class="cat-banner-card__name">Courses</h3>
          <p class="cat-banner-card__count">Online Skills &amp; Training</p>
        </div>
        <div class="cat-banner-card__img">
          <img src="https://images.unsplash.com/photo-1434030216411-0b793f4b4173?w=250&q=80" alt="Courses">
        </div>
      </a>

    </div>
  </div>
</div>

<!-- Buy Modal -->
<div id="buyModal" class="modal-overlay" role="dialog" aria-modal="true">
  <div class="modal-box">
    <div class="modal-box__header">
      <h3 class="modal-title">Enter Phone Number</h3>
      <button class="modal-close" id="cancelBtn" aria-label="Close">
        <i data-lucide="x"></i>
      </button>
    </div>
    <p class="modal-subtitle">Enter the active number to receive the data bundle</p>
    <div class="form-group">
      <label class="form-label">Phone Number</label>
      <div class="input-wrapper">
        <i data-lucide="phone"></i>
        <input type="text" id="phoneInput" class="form-input"
               placeholder="e.g 08012345678" maxlength="11" inputmode="numeric">
      </div>
      <small id="phoneError" style="color:var(--error);display:none;margin-top:4px;">
        Phone number must be exactly 11 digits
      </small>
    </div>
    <div class="modal-actions">
      <button id="checkoutBtn" class="btn btn--primary btn--full">
        <i data-lucide="arrow-right"></i>
        Proceed to Checkout
      </button>
    </div>
  </div>
</div>

<script>
/* ── Tab indicator ─────────────────────────────────────── */
function moveTabIndicator(tab) {
  var indicator = document.getElementById('tabIndicator');
  var btn = document.getElementById(tab === 'home' ? 'tabHome' : 'tabCategory');
  if (!indicator || !btn) return;
  indicator.style.left  = btn.offsetLeft + 'px';
  indicator.style.width = btn.offsetWidth + 'px';
}

/* ── Tab switching ─────────────────────────────────────── */
function switchTab(tab) {
  var isHome  = tab === 'home';
  var showEl  = document.getElementById(isHome ? 'homeContent' : 'categoryContent');
  var hideEl  = document.getElementById(isHome ? 'categoryContent' : 'homeContent');

  hideEl.style.display = 'none';
  hideEl.classList.remove('tab-entering');

  showEl.style.display = '';
  showEl.classList.remove('tab-entering');
  void showEl.offsetWidth;
  showEl.classList.add('tab-entering');

  document.getElementById('tabHome').classList.toggle('active', isHome);
  document.getElementById('tabCategory').classList.toggle('active', !isHome);

  moveTabIndicator(tab);
  lucide.createIcons();
}

// Initialise indicator on page load (no transition on first paint)
(function() {
  var indicator = document.getElementById('tabIndicator');
  if (indicator) indicator.style.transition = 'none';
  moveTabIndicator('home');
  setTimeout(function() {
    if (indicator) indicator.style.transition = '';
  }, 50);
})();

/* ── Carousel ─────────────────────────────────────────── */
(function() {
  var slides  = document.querySelectorAll('.promo-slide');
  var dots    = document.querySelectorAll('#promoDots .promo-dot');
  var current = 0;
  var timer;

  function go(n) {
    slides[current].classList.remove('active');
    dots[current].classList.remove('promo-dot--active');
    current = (n + slides.length) % slides.length;
    slides[current].classList.add('active');
    dots[current].classList.add('promo-dot--active');
    var badgeNum = document.getElementById('slideBadgeNum');
    if (badgeNum) badgeNum.textContent = String(current + 1).padStart(2, '0');
  }

  function restart() {
    clearInterval(timer);
    timer = setInterval(function() { go(current + 1); }, 4500);
  }

  dots.forEach(function(dot, i) {
    dot.addEventListener('click', function() { go(i); restart(); });
  });

  restart();
})();

/* ── Cat-browse scroll-reveal ─────────────────────────── */
(function() {
  var heading = document.querySelector('.cat-browse__heading');
  var grid    = document.querySelector('.cat-browse__grid');
  if (!grid || !window.IntersectionObserver) return;

  var io = new IntersectionObserver(function(entries) {
    entries.forEach(function(entry) {
      if (!entry.isIntersecting) return;
      if (entry.target === heading && heading) {
        heading.classList.add('revealed');
      } else {
        entry.target.querySelectorAll('.cat-browse__card').forEach(function(card) {
          card.classList.add('revealed');
        });
        io.unobserve(entry.target);
      }
    });
  }, { threshold: 0.15 });

  if (heading) io.observe(heading);
  io.observe(grid);

  lucide.createIcons();
})();

/* ── Buy modal ────────────────────────────────────────── */
var selectedPackageId = null;

document.querySelectorAll('.buy-btn').forEach(function(btn) {
  btn.addEventListener('click', function(e) {
    e.preventDefault();
    e.stopPropagation();
    selectedPackageId = this.dataset.id;
    document.getElementById('buyModal').classList.add('open');
  });
});

document.getElementById('cancelBtn').addEventListener('click', function() {
  document.getElementById('buyModal').classList.remove('open');
});

document.getElementById('buyModal').addEventListener('click', function(e) {
  if (e.target === this) this.classList.remove('open');
});

var phoneInput = document.getElementById('phoneInput');
var phoneError = document.getElementById('phoneError');

phoneInput.addEventListener('input', function() {
  this.value = this.value.replace(/\D/g, '').slice(0, 11);
});

document.getElementById('checkoutBtn').addEventListener('click', async function() {
  var phone = phoneInput.value.trim();
  if (!/^\d{11}$/.test(phone)) { phoneError.style.display = 'block'; return; }
  phoneError.style.display = 'none';
  if (!selectedPackageId) { Swal.fire({ icon: 'error', text: 'No product selected' }); return; }
  try {
    var res  = await fetch('/checkout/initiate', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ packageId: selectedPackageId, phone: phone })
    });
    var data = await res.json();
    if (data.success) {
      window.location.href = '/checkout';
    } else {
      Swal.fire({ icon: 'error', text: data.message || 'Something went wrong' });
    }
  } catch (err) {
    Swal.fire({ icon: 'error', text: 'Network error' });
  }
});
</script>
