
  <div class="loader"></div>
  <div id="app">
    <div class="main-wrapper main-wrapper-1">
      <div class="navbar-bg"></div>
      <%- include('../partials/headerAdmin.ejs')%>
      <%- include('../partials/sideMenuAdmin.ejs')%>

      <div class="main-content">
        <section class="section">
          <div class="section-body">

            <div class="row">
              <div class="col-12 col-lg-8 mx-auto">

                <% if (typeof query !== 'undefined' && query.saved == '1') { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                  Settings saved successfully.
                  <button type="button" class="close" data-dismiss="alert">&times;</button>
                </div>
                <% } %>

                <% if (typeof query !== 'undefined' && query.error == '1') { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                  An error occurred. Please try again.
                  <button type="button" class="close" data-dismiss="alert">&times;</button>
                </div>
                <% } %>

                <form method="post" action="/admin/settings" id="settingsForm">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                  <!-- RP Transfer to BitToken -->
                  <div class="card">
                    <div class="card-header">
                      <h4><i data-feather="repeat" style="width:18px;height:18px;margin-right:6px;"></i> BitToken RP Transfer</h4>
                    </div>
                    <div class="card-body">

                      <div class="form-group d-flex align-items-center justify-content-between" style="border-bottom:1px solid #f0f0f0;padding-bottom:16px;margin-bottom:20px;">
                        <div>
                          <label class="mb-0" style="font-weight:600;">Allow RP Transfer to BitToken</label>
                          <p class="text-muted mb-0" style="font-size:13px;">When disabled, users will see a notice instead of being able to transfer their Reward Points.</p>
                        </div>
                        <div class="ml-4">
                          <label class="custom-switch" style="margin-bottom:0;">
                            <input type="checkbox" id="rpTransferToggle" class="custom-switch-input"
                              <%= settings.rpTransferEnabled ? 'checked' : '' %>>
                            <span class="custom-switch-indicator"></span>
                          </label>
                          <!-- Hidden field carries the actual value since unchecked checkboxes are not submitted -->
                          <input type="hidden" name="rpTransferEnabled" id="rpTransferEnabledInput"
                            value="<%= settings.rpTransferEnabled ? 'true' : 'false' %>">
                        </div>
                      </div>

                      <div class="form-group mb-0">
                        <label for="rpTransferSuspendedMessage" style="font-weight:600;">Suspension Notice Message</label>
                        <p class="text-muted" style="font-size:13px;">This message is shown to users when RP transfer is disabled.</p>
                        <textarea
                          name="rpTransferSuspendedMessage"
                          id="rpTransferSuspendedMessage"
                          class="form-control"
                          rows="3"
                          maxlength="500"
                          placeholder="e.g. RP transfer has been suspended for the time being. Please check back later."
                        ><%= settings.rpTransferSuspendedMessage %></textarea>
                        <small class="text-muted"><span id="charCount"><%= settings.rpTransferSuspendedMessage.length %></span> / 500 characters</small>
                      </div>

                    </div>
                  <!-- Maintenance Mode -->
                  <div class="card">
                    <div class="card-header">
                      <h4><i data-feather="tool" style="width:18px;height:18px;margin-right:6px;"></i> Maintenance Mode</h4>
                    </div>
                    <div class="card-body">

                      <div class="form-group d-flex align-items-center justify-content-between" style="border-bottom:1px solid #f0f0f0;padding-bottom:16px;margin-bottom:20px;">
                        <div>
                          <label class="mb-0" style="font-weight:600;">Enable Maintenance Mode</label>
                          <p class="text-muted mb-0" style="font-size:13px;">When enabled, all public pages will display the maintenance screen. The admin panel remains accessible.</p>
                        </div>
                        <div class="ml-4">
                          <label class="custom-switch" style="margin-bottom:0;">
                            <input type="checkbox" id="maintModeToggle" class="custom-switch-input"
                              <%= settings.maintenanceModeEnabled ? 'checked' : '' %>>
                            <span class="custom-switch-indicator"></span>
                          </label>
                          <input type="hidden" name="maintenanceModeEnabled" id="maintModeEnabledInput"
                            value="<%= settings.maintenanceModeEnabled ? 'true' : 'false' %>">
                        </div>
                      </div>

                      <div class="form-group mb-0">
                        <label for="maintenanceMessage" style="font-weight:600;">Maintenance Page Message</label>
                        <p class="text-muted" style="font-size:13px;">This message is displayed on the maintenance screen shown to users.</p>
                        <textarea
                          name="maintenanceMessage"
                          id="maintenanceMessage"
                          class="form-control"
                          rows="3"
                          maxlength="600"
                          placeholder="e.g. We're performing scheduled maintenance. We'll be back shortly."
                        ><%= settings.maintenanceMessage %></textarea>
                        <small class="text-muted"><span id="maintMsgCount"><%= settings.maintenanceMessage.length %></span> / 600 characters</small>
                      </div>

                    </div>
                  </div>

                  <!-- Upcoming Maintenance Banner -->
                  <div class="card">
                    <div class="card-header">
                      <h4><i data-feather="bell" style="width:18px;height:18px;margin-right:6px;"></i> Upcoming Maintenance Banner</h4>
                    </div>
                    <div class="card-body">

                      <div class="form-group d-flex align-items-center justify-content-between" style="border-bottom:1px solid #f0f0f0;padding-bottom:16px;margin-bottom:20px;">
                        <div>
                          <label class="mb-0" style="font-weight:600;">Show Countdown Banner</label>
                          <p class="text-muted mb-0" style="font-size:13px;">Displays a banner at the top of every public page counting down to the scheduled maintenance time.</p>
                        </div>
                        <div class="ml-4">
                          <label class="custom-switch" style="margin-bottom:0;">
                            <input type="checkbox" id="maintBannerToggle" class="custom-switch-input"
                              <%= settings.maintenanceBannerEnabled ? 'checked' : '' %>>
                            <span class="custom-switch-indicator"></span>
                          </label>
                          <input type="hidden" name="maintenanceBannerEnabled" id="maintBannerEnabledInput"
                            value="<%= settings.maintenanceBannerEnabled ? 'true' : 'false' %>">
                        </div>
                      </div>

                      <div class="form-group mb-0">
                        <label for="maintenanceBannerScheduledAt" style="font-weight:600;">Scheduled Maintenance Time</label>
                        <p class="text-muted" style="font-size:13px;">The countdown banner will display until this date and time is reached.</p>
                        <input
                          type="datetime-local"
                          name="maintenanceBannerScheduledAt"
                          id="maintenanceBannerScheduledAt"
                          class="form-control"
                          style="max-width:320px;"
                          value="<%= settings.maintenanceBannerScheduledAt ? new Date(settings.maintenanceBannerScheduledAt).toISOString().slice(0,16) : '' %>"
                        >
                      </div>

                    </div>
                    <div class="card-footer text-right">
                      <button type="button" class="btn btn-primary" id="saveSettingsBtn">
                        <i class="fa fa-save mr-1"></i> Save Settings
                      </button>
                    </div>
                  </div>

                </form>

              </div>
            </div>

          </div>
        </section>

        <div class="settingSidebar">
          <a href="javascript:void(0)" class="settingPanelToggle"><i class="fa fa-spin fa-cog"></i></a>
          <div class="settingSidebar-body ps-container ps-theme-default">
            <div class="fade show active">
              <div class="setting-panel-header">Setting Panel</div>
              <div class="p-15 border-bottom">
                <h6 class="font-medium m-b-10">Select Layout</h6>
                <div class="selectgroup layout-color w-50">
                  <label class="selectgroup-item">
                    <input type="radio" name="value" value="1" class="selectgroup-input-radio select-layout" checked>
                    <span class="selectgroup-button">Light</span>
                  </label>
                  <label class="selectgroup-item">
                    <input type="radio" name="value" value="2" class="selectgroup-input-radio select-layout">
                    <span class="selectgroup-button">Dark</span>
                  </label>
                </div>
              </div>
              <div class="mt-4 mb-4 p-3 align-center rt-sidebar-last-ele">
                <a href="#" class="btn btn-icon icon-left btn-primary btn-restore-theme">
                  <i class="fas fa-undo"></i> Restore Default
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>

      <footer class="main-footer">
        <div class="footer-left">&copy; <%= new Date().getFullYear() %> Kabacu. All rights reserved.</div>
        <div class="footer-right"></div>
      </footer>
    </div>
  </div>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
// RP Transfer toggle
document.getElementById('rpTransferToggle').addEventListener('change', function () {
  document.getElementById('rpTransferEnabledInput').value = this.checked ? 'true' : 'false';
});
document.getElementById('rpTransferSuspendedMessage').addEventListener('input', function () {
  document.getElementById('charCount').textContent = this.value.length;
});

// Maintenance mode toggle
document.getElementById('maintModeToggle').addEventListener('change', function () {
  document.getElementById('maintModeEnabledInput').value = this.checked ? 'true' : 'false';
});
document.getElementById('maintenanceMessage').addEventListener('input', function () {
  document.getElementById('maintMsgCount').textContent = this.value.length;
});

// Maintenance banner toggle
document.getElementById('maintBannerToggle').addEventListener('change', function () {
  document.getElementById('maintBannerEnabledInput').value = this.checked ? 'true' : 'false';
});

// Save confirmation
document.getElementById('saveSettingsBtn').addEventListener('click', function () {
  Swal.fire({
    title: 'Save Settings?',
    text: 'This will update the live site configuration immediately.',
    icon: 'question',
    showCancelButton: true,
    confirmButtonText: 'Yes, Save',
    confirmButtonColor: '#47c363',
    cancelButtonText: 'Cancel',
  }).then(function (result) {
    if (result.isConfirmed) {
      document.getElementById('settingsForm').submit();
    }
  });
});
</script>
